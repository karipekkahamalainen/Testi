---
- name: Ping AWX Gitistä tehtava1(muokattu)
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Ping localhost
      ansible.builtin.ping:

    - name: Tulosta viesti
      ansible.builtin.debug:
        msg: "Playbook suoritettiin onnistuneesti!"
